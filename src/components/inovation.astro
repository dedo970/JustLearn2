---
import { Box } from '@chakra-ui/react'
import { Container, Text, } from '@chakra-ui/react'
import Quotes from '../lib/quotes.json'
import { FaQuoteLeft } from "react-icons/fa"

  const Quote = Quotes.sort(() => Math.random() - Math.random()).find(() => true ) ;
---
<div class='flex flex-col justify-center items-center text-white bg-gradient-to-b from-transparent to-slate-900 h-80 mt-24'>
    {/* background= {'linear-gradient(0deg, rgba(6, 2, 13, 1) 0%, rgba(238, 130, 238, 0) 100%)'}
    // bg={'#01C38D'}  */}
    {/* h={'300px'} */}
    {/* mt={'6em'}  */}
    {/* display={'flex'} 
    flexDirection={'column'}
    justifyContent={'center'}
    alignItems={'center'}
    color={'white'}> */}
                  <div class='container'>
                   {Quote &&
           <FaQuoteLeft style={{ height: '60px', width: '60px', margin: '10px 10px 20px 10px' }}/>
        <div class='text-left m-2 font-semibold text-xl'>&quot;{Quote.quote}&quot;</div>
        <div class='text-right m-2 font-semibold'>{Quote.author}</div>
        <div class='flex justify-end'><FaQuoteLeft style={{ height: '40px', width: '40px', marginRight: '10px', marginTop: '20px' }}/> </div>
        
      }

  </div>
</div>